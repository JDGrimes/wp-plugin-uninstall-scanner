# Travis CI configuration file.

language: php

php:
    - 5.5
    - 5.6
    - 7.0
    - 7.1
    - 7.2

sudo: false

matrix:
  include:
    - php: nightly
  allow_failures:
    - php: nightly
  fast_finish: true

before_script:
    - composer install

script:
    - phpunit

